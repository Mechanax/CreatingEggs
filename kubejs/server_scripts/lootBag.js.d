/*ItemEvents.rightClicked('kubejs:loot_bag_white', event => {
    event.item.shrink(1);
    const items = [];
    const randomIndex = Math.floor(Math.random() * items.length);
    const selectedItem = items[randomIndex];
    event.player.give(selectedItem);
});*/